# ===========================
# Automation Suite - Configuración
# ===========================
#
# ⚠️ IMPORTANTE - SEGURIDAD:
# 1. Este archivo contiene credenciales y configuraciones sensibles
# 2. NUNCA debe ser commiteado al repositorio (está en .gitignore)
# 3. Cada desarrollador debe crear su propio archivo .env desde este ejemplo
# 4. Para crear tu archivo .env: cp .env.example .env
# 5. CAMBIA las credenciales por defecto en producción
#

# ===========================
# Backend Configuration
# ===========================

# Base de datos
# SQLite (desarrollo)
DATABASE_URL=sqlite+aiosqlite:///./automation.db
# PostgreSQL (producción)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/automation_suite

# Seguridad
# ⚠️ CAMBIAR en producción: Genera una clave aleatoria segura (mínimo 32 caracteres)
SECRET_KEY=change_me_in_production_to_a_random_secret_key
# TELEMETRY_TOKEN=  # Opcional: token para proteger endpoint de telemetría (descomentar para habilitar)

# Admin por defecto (se crea al iniciar si no hay admins)
# ⚠️ CAMBIAR en producción: Usa credenciales fuertes y únicas
ADMIN_DEFAULT_USER=admin
ADMIN_DEFAULT_PASS=admin123

# CORS (permitir acceso desde el portal)
CORS_ALLOW_ORIGIN=http://localhost:8600

# Sesión
SESSION_EXPIRE_HOURS=8

# Cache
CATALOG_CACHE_TTL=15  # Segundos

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60  # Segundos

# Servidor
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8601

# Seed (opcional)
# APPS_REGISTRY_PATH=portal/apps_registry.yaml

# ===========================
# Portal Configuration
# ===========================

# URL del backend
BACKEND_BASE_URL=http://localhost:8601

# Path base del portal (para proxy/subpath)
PORTAL_BASE_PATH=/portal

# ===========================
# Producción
# ===========================

# Para despliegue en producción, considera:
# - Usar PostgreSQL en lugar de SQLite
# - Generar un SECRET_KEY fuerte y aleatorio
# - Cambiar las credenciales por defecto del admin
# - Habilitar HTTPS (cambiar URLs a https://)
# - Configurar CORS_ALLOW_ORIGIN con el dominio correcto
# - Establecer TELEMETRY_TOKEN para proteger la ingesta de eventos
